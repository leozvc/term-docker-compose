nginx001:
        image: nginx
        volumes:
                - ./proxy.conf:/etc/nginx/conf.d/default.conf
        ports:
                - "8081:80"
        environment:
                - NGINX_HOST=106.186.28.242
                - NGINX_PORT=80
        command: /bin/bash -c "nginx -g 'daemon off;'"
